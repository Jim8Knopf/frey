# Unsloth Fine-Tuning Requirements
# Run on a GPU machine (not the Raspberry Pi!)

# Core dependencies
torch>=2.1.0
transformers>=4.36.0
datasets>=2.14.0
trl>=0.7.0
peft>=0.7.0
accelerate>=0.25.0
bitsandbytes>=0.41.0

# Unsloth (install from source for latest optimizations)
# Install with: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# Optional but recommended
nvidia-ml-py3  # GPU monitoring
wandb  # Experiment tracking (optional)
